<?xml version="1.0" encoding="utf-8"?>
<!--
  Дозволяємо незашифроване з'єднання ТІЛЬКИ з локальним комп'ютером:
  127.0.0.1 (через `adb reverse`) і типові адреси домашньої мережі.
  Решта інтернету лишається під звичайними правилами (лише HTTPS).
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>

    <!-- домашня мережа: 192.168.x.x / 10.x.x.x задаються користувачем -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
